[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # FastAPI usa Depends() en defaults

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
